/*
** Fichier :
** parser.cpp
** Description
** Fichier servant à parser le fichier script souhaité
*/

#include "parser.h"
using namespace std;

Parser::Parser()
{

}

string Parser::parseFunction(ifstream path, string m_function)
{
	string functionName[0];
	size = new int;
	char delim(')');
	char preDelim('(');

	if (stream)
	{
		for (size(0); size >= functionName[0] && size != delim - 2; size++)
		{
			stream.get(functionName, size, delim);
		}
		cout << "Done" << endl;
	}
	else
	{
		cout << "Erreur : Le fichier n'existe pas, parsing de fonction impossible" << endl;
		return 84;
	}

	delete size;
	return m_function;
}

Parser::~Parser()
{

}
