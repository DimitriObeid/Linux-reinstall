#!/usr/bin/env bash

# TODO : Une fois le dépôt Git contenant les librairies créé (une fois le script passé en version stable 2.0), télécharger le dépôt via l'outil "wget".

$path=

# Installing library files in /usr/local/bin directory
if test ! -d 
